# TODO / 칸반 — 알고핑 (AlGoPing)

[PRD.md](PRD.md) §15 단계별 구현 순서를 기준으로 한다. 항목을 완료할 때마다 체크하고, 진행 중인 항목은 `[~]`로 표시한다.

## Phase 0 — 프로젝트 셋업
- [x] 작업 폴더 구조 확정 (`server.js`, `public/`, `data/`) — `storage.js`/`github-suggest.js`는 Phase 4/7에서 추가
- [x] package.json 및 기본 의존성 설치 (express)
- [x] `data/problems.json` 초기 스키마 파일 생성
- [x] 서버 기동 확인 (`GET /` 응답, 정적 파일 서빙)

## Phase 1 — 핵심 MVP: 오늘의 문제 조회 (알림 없음)
- [x] 기업별 대표 문제 29개 `problems.json`에 초기 입력 (`id, company, name, plat, level, tags, url, exp`) — 원본 문제 본문 복제 없이 자체 작성 요약(exp)만 사용
  - 최초 초안은 백준 23개 + 프로그래머스 6개였으나, 백준이 2026-04-28부로 서비스 종료된 것을 확인해 백준 23개를 전부 프로그래머스의 살아있는 문제로 교체(브라우저로 각 링크 실접속 확인 후 반영)
  - 리트코드 9문제(기업군별 1개씩) 추가, 전부 실접속 확인 후 반영 → 현재 총 38개(프로그래머스 29 / 리트코드 9)
  - 코드시그널·코드트리는 로그인 없이 개별 문제 직링크가 확인되지 않아 이번 범위에서는 보류(PRD §12 참고)
- [x] 날짜 기반 순차 로테이션 로직 구현
- [x] `GET /api/today` 구현 (필터 없이 전체 문제 대상)
- [x] 최소 프론트엔드: 오늘의 문제 카드(기업/문제명/플랫폼/난이도/태그/exp/링크) 표시
- [x] **검증**: 브라우저 접속 시 오늘의 문제가 정상 표시되고, 링크가 원본 문제 페이지로 정확히 연결됨을 확인 (2026-07-23 기준)

## Phase 2 — 사용자별 설정 및 필터
- [x] 브라우저 익명 `clientId` 발급(crypto.randomUUID) 및 localStorage 저장 로직
- [x] `GET /api/settings` / `POST /api/settings` 구현 (없으면 기본값 생성) — 파일 기반 임시 저장(`data/subscribers.json`, Phase 4에서 storage.js로 정식 추상화 예정)
- [x] `GET /api/meta` 추가 (기업/태그/난이도 목록 — 필터 UI 동적 구성용)
- [x] 기업/태그/난이도 다중 선택 필터 UI(칩 토글) + 서버 반영
- [x] 출제 순서 옵션: 순차 / 랜덤(날짜+clientId 시드) / 안 푼 우선(solvedIds는 Phase 3에서 실제로 채워짐)
- [x] 필터에 맞는 문제가 없을 때 전체 문제로 대체 선택
- [x] **검증**: 실제 브라우저 클릭으로 카카오 필터 선택 → 저장 → `/api/today`가 카카오 문제만 반환하고 카드도 즉시 갱신되는 것을 확인 (curl로 서버 저장값도 재확인)

## Phase 3 — 오늘의 문제 상호작용
- [x] `POST /api/skip-today` (같은 날 안에서 skipSeed를 누적해 동일 조건 풀에서 매번 다른 문제로 재선정)
- [x] `POST /api/mark-solved` (solvedIds 반영, `/api/today` 응답에 `solved` 플래그 포함)
- [x] `POST /api/reset-progress` (solvedIds 초기화)
- [x] 프론트엔드: 건너뛰기/풀었음 체크/기록 초기화 버튼 + 풀었음 상태 시각 표시(초록 배지)
- [x] **검증**: curl로 건너뛰기 연속 호출 시 문제가 계속 바뀌고 `/api/today`도 같은 결과를 반환하는지, 풀었음 체크 후 "안 푼 우선" 모드에서 그 문제가 제외되는지, 초기화 후 원상복구되는지 확인. 브라우저 실제 클릭으로 건너뛰기/풀었음 체크/새로고침 후 상태 유지까지 재확인
- [x] (추가 요청) 화면을 오늘의 문제 / 풀이 기록 / 설정 3개 페이지로 분리 — `public/style.css`, `public/app.js` 공용화 + 상단 네비게이션 추가, `GET /api/solved` 신규 엔드포인트로 풀이 기록 페이지에 문제 상세 목록 표시
- [x] **검증**: 세 페이지 모두 200 응답 확인, 브라우저에서 실제 탭 클릭으로 이동 확인, 풀이 기록 페이지에 이전에 체크한 문제가 정확히 표시되고 설정 페이지에도 이전에 저장한 필터(카카오)가 유지되는 것 확인
- [x] (추가 요청) 반응형 레이아웃 — 700px 이상에서 컬럼 폭/패딩/폰트 확대하는 미디어 쿼리 추가
- [x] (추가 요청) 다크모드 — 모든 색상을 CSS 변수화하고 `prefers-color-scheme: dark`에 맞춰 팔레트 전환
- [x] (추가 요청) "문제 풀러 가기" 옆에 난이도 기반 예상 풀이 시간 칩 추가 (`link-row` + `estimate-chip`)
- [x] **검증**: 모바일(375x812)·데스크톱(1280x800) × 라이트/다크 총 4가지 조합을 브라우저로 직접 렌더링해 확인 (오늘의 문제/설정 페이지 모두)
- [x] (추가 요청) 풀이 기록 페이지에서 각 문제의 "설명 보기" 토글 버튼 추가 — 클릭 시 핵심 포인트/풀이 접근이 펼쳐지고, 다시 누르면 접힘
- [x] **검증**: 브라우저 실제 클릭으로 펼침 → 버튼 텍스트 "설명 접기"로 변경 확인 → 다시 클릭해 접힘까지 확인

## Phase 4 — 데이터 영속성
- [x] `storage.js` 저장소 추상화 인터페이스 설계 (`getOrCreateSubscriber`, `saveSettings`, `saveSkipState`, `addSolved`, `clearSolved`)
- [x] 로컬 JSON 파일 폴백 구현 (`DATABASE_URL` 미설정 시, `data/subscribers.json`)
- [x] Postgres `subscribers` 테이블 설계 및 연동 (`DATABASE_URL` 존재 시 `pg` 패키지로 연결, 최초 요청 시 `CREATE TABLE IF NOT EXISTS`)
- [x] Homebrew로 로컬 Postgres 16 설치 + 테스트 DB(`coding_daily_dev`) 생성해 실제 연동 검증 (Neon 계정 없이도 코드 경로 확인 가능)
- [x] **검증**: `DATABASE_URL` 없이(파일 모드)·있이(Postgres 모드) 각각 설정 저장/풀이 체크/건너뛰기/초기화를 curl로 실행하고, `psql`로 테이블을 직접 조회해 JSON이 올바른 구조로 저장됐는지 확인. 두 모드 모두 서버 프로세스 재시작 후에도 값이 유지되는 것 확인

## Phase 5 — PWA / Web Push 인프라
- [x] `manifest.json` + 아이콘 세트 준비 (`icon-192.png`, `icon-512.png`, `apple-touch-icon.png`, `favicon-32.png` — SVG 원본을 `qlmanage`/`sips`로 래스터화해 생성) + 3개 페이지 `<head>`에 manifest/apple-touch-icon/theme-color 링크 추가
- [x] 서비스 워커(`public/sw.js`): 푸시 수신 시 알림 표시 + 알림 클릭 시 해당 URL로 포커스/이동
- [x] `vapid.js`로 VAPID 키 쌍을 최초 1회 생성 후 `data/vapid.json`에 고정 저장(재시작해도 동일 키 유지 — 키가 바뀌면 기존 구독이 전부 무효화되므로 중요)
- [x] `GET /api/vapid-public-key`, `POST /api/subscribe`, `POST /api/unsubscribe` 구현, `storage.js`에 `subscription` 필드 추가(파일/Postgres 양쪽)
- [x] 설정 페이지에 "알림 켜기/끄기" UI + 상태 표시 + "테스트 알림 보내기" 버튼 추가
- [x] (Phase 6에서 가져옴) `POST /api/resend-today` — 구독된 사용자에게 오늘의 문제를 즉시 발송하는 테스트/재발송용 엔드포인트
- [x] **검증(서버 로직)**: curl로 가짜 구독 정보를 등록 → `/api/resend-today` 호출 시 실제로 `web-push`가 발송을 시도하고 실패를 정상적으로 에러 응답하는지 확인, `/api/unsubscribe` 후 구독 정보가 null로 초기화되는지 확인, 서버가 크래시 없이 안정적인지 확인
- [ ] **미검증(환경 제약)**: 실제 브라우저 알림 권한 허용 → 진짜 구독 생성 → 실기기 알림 수신 — 이 자동화 브라우저는 정책상 Notification 권한을 자동 거부하므로 사용자가 실제 Mac 브라우저 또는 iPhone에서 직접 확인 필요
- [x] (추가 요청) `data/algorithms.json`에 문제 태그(26종) 전부에 대한 자체 작성 알고리즘 설명 추가, `GET /api/algorithms(?tags=)` 구현
- [x] **재작업**: "알고리즘"을 오늘의 문제/풀이 기록/설정과 동급인 4번째 상단 탭으로 승격 (`public/algorithm.html`, 새 탭이 아닌 인앱 이동으로 변경)
  - 탭에 직접 들어가면 26개 유형 칩 전체를 볼 수 있고, 칩을 클릭하면 아코디언 카드가 펼쳐짐(유형별 브라우징)
  - 문제 카드의 "🧠 알고리즘 보기" 버튼으로 들어오면 `?tags=` 쿼리로 해당 문제의 태그가 자동 선택·펼침·스크롤됨
  - 오늘의 문제 카드: "문제 풀러 가기"와 "🧠 알고리즘 보기"를 `link-row`에서 50/50 크기 버튼으로 배치(기존 텍스트 링크 제거)
  - 예상 풀이 시간(⏱)을 문제 풀러가기 옆에서 제목 근처 태그(meta) 줄로 이동
- [x] **검증**: `/api/algorithms` 태그 필터 curl 확인, 브라우저에서 오늘의 문제 카드 버튼 50/50 레이아웃과 태그 줄의 예상 시간 확인, 알고리즘 탭 직접 진입 시 전체 브라우징 및 칩 클릭 시 카드 펼침 확인, 문제에서 버튼으로 진입 시 해당 태그 칩 선택 상태 + 카드 자동 펼침·스크롤 확인 (라이트 모드)
- [x] (추가 요청) 알고리즘 탭의 "궁금한 유형을 선택하면..." 안내 문구와 칩 목록이 겹쳐 보이던 문제 수정 — `.group-label`이 `.settings-group` 래퍼에만 종속되던 CSS를 일반화하고, `.algo-picker`에 안내 문구와 칩 사이 구분선(border-bottom)을 추가
- [x] (추가 요청) 서비스명을 "알고핑 AlGoPing"으로 확정 — 모든 페이지에 로고 아이콘 + 워드마크(`algo` + 강조색 `ping`) 헤더 추가, `<title>`/manifest.json/apple-mobile-web-app-title 전부 반영, PRD·TODO·package.json 제목도 함께 갱신
- [x] (추가 요청) 라이트 모드는 Color Hunt `6367ff/8494ff/c9beff/ffdbfd`, 다크 모드는 `3c467b/50589c/636ccb/6e8cfb` 팔레트로 전면 재도색 — `style.css`의 모든 색상을 CSS 변수로 관리하던 구조 덕분에 두 미디어 쿼리 블록만 교체
- [x] **검증**: 모바일·데스크톱 크기에서 라이트/다크 모드 각각 오늘의 문제/설정/풀이 기록/알고리즘 4개 페이지 전부 스크린샷으로 확인, 배색이 깨지거나 대비가 나쁜 곳 없음을 확인
- [x] **재조정(사용자 피드백)**: 라이트 모드 배경(#ffdbfd)이 눈부시다는 피드백 → 팔레트 색은 버튼·강조 요소에만 남기고 배경은 `#f6f5fa`(옅은 무채색)로 완화. 다크 모드는 글자가 안 보인다는 피드백 → 원인은 칩 배경(`--chip-bg`)이 텍스트와 명도 차이가 부족했던 것 → 칩 배경을 훨씬 어둡게(`#2a2e5c`) 낮춰 대비 확보
- [x] **재조정**: 로고 자리에 아직 디자인이 없어 임시로 🔔 이모지 사용, 페이지별 서브타이틀(날짜/오늘의 문제 설정/풀이 기록/알고리즘 사전)을 제목 아래 별도 줄 대신 제목과 같은 줄 오른쪽 끝에 배치(`margin-left:auto`)해서 제목이 작아 보이지 않도록 수정
- [x] **검증**: 라이트/다크 모드 스크린샷 재확인 — 배경 편안함, 다크모드 배지·태그·버튼 텍스트 전부 명확히 보임, 헤더가 한 줄로 정리된 것 확인

## Phase 6 — 알림 스케줄링 자동화
- [x] `storage.js`/`settings` 확장: `notifyTime`(HH:MM), `notifyDays`(0=일~6=토 배열), `problemsPerDay`(1~3), `paused`(`{active, until}`) 필드 추가 — 기존 구독자도 `normalize()`가 기본값을 병합해 하위 호환
- [x] 서버 시간 계산을 KST(Asia/Seoul) 기준으로 통일 — 배포 서버가 UTC를 쓰더라도 "오늘의 문제"가 바뀌는 시점과 알림 발송 시각 판정이 항상 한국 시간 기준이 되도록 `seoulNow()` 헬퍼 추가, 기존 `todayDateString()`도 이걸로 교체
- [x] `POST /api/send-daily` 발송 대상 판정 로직 — 구독자 전체를 순회하며 구독 여부 → 일시정지(만료 시 자동 해제) → 알림 요일 → 알림 시각 → 오늘 이미 발송했는지(`state.lastSentDate`) 순서로 필터링, 통과한 사용자에게만 `problemsPerDay`개의 문제를 묶어 발송
- [x] `POST /api/resend-today` (Phase 5에서 이미 구현됨 — 조건 없이 즉시 재발송하는 수동 테스트용)
- [x] 크론 엔드포인트 시크릿 키 보호 — `CRON_SECRET` 환경변수가 설정된 경우에만 `Authorization: Bearer` 헤더 또는 `?secret=` 쿼리로 인증, 미설정 시(로컬 개발) 인증 없이 동작
- [x] GitHub Actions 15분 주기 트리거 스캐폴딩 (`.github/workflows/send-daily.yml`) — `APP_URL`/`CRON_SECRET` 시크릿은 Phase 8 배포 시 등록 필요. `.env.example`에 필요한 환경변수 문서화
- [x] 설정 페이지에 알림 시간(time input)/알림 요일(칩)/하루 문제 수(select)/일시정지(토글+날짜) UI 추가
- [x] **검증**: curl로 아래 5가지 케이스를 개별적으로 확인 — (A) 알림 요일에 오늘이 없으면 조용히 건너뜀 (B) 알림 시각이 아직 안 지났으면 건너뜀 (C) 일시정지 중이면 건너뜀 (D) 모든 조건을 통과하면 실제로 `web-push` 발송을 시도함(로그로 확인) (E) 오늘 이미 발송된 상태(`lastSentDate`)면 중복 발송하지 않음. 일시정지 `until` 날짜가 지나면 자동 해제되는 것도 확인. `CRON_SECRET` 설정 시 401 처리와 올바른 시크릿(헤더/쿼리 둘 다) 통과도 확인. 브라우저에서 실제 클릭으로 요일 선택("평일만" 등)·시간·문제 수·일시정지 토글이 저장되고 서버에 정확히 반영되는 것 확인
- [ ] **미검증(환경 제약)**: 실제 다수 사용자에게 실제 브라우저 알림이 목표 시각 기준 30분 이내에 도착하는지는 Phase 5와 같은 이유로 이 자동화 환경에서 확인 불가 — 실기기/실브라우저 및 실제 배포(Phase 8) 후 확인 필요
- [x] (추가 요청) 상단 탭 순서를 오늘의 문제/풀이 기록/알고리즘/설정으로 변경 (기존: 오늘의 문제/풀이 기록/설정/알고리즘) — 4개 페이지 전부 반영
- [x] (추가 요청) 풀이 기록 상세의 "이 알고리즘이 낯설다면 설명 보기" 텍스트 링크를 오늘의 문제 카드와 같은 `.link-btn.secondary` 버튼 스타일로 교체, 어떻게 풀까 목록과의 간격 정리(margin 조정), 사용하지 않게 된 `.algo-link` CSS 제거
- [x] **검증**: 브라우저에서 4개 페이지 모두 탭 순서·active 표시 확인, 풀이 기록에서 버튼 스타일/간격/링크 정확성을 라이트·다크 모드 모두 확인
- [x] (추가 요청) 알고리즘 26개 전부에 자체 제작 인라인 SVG 다이어그램 추가 (`data/algorithms.json`의 `image` 필드) — 페이지 CSS 변수(`var(--accent)` 등)를 그대로 참조해서 라이트/다크 테마 전환 시 다이어그램 색도 자동으로 맞춰짐. 외부 이미지 파일이 아니라 인라인 SVG라 가능한 방식 (외부 `<img src="*.svg">`는 부모 페이지의 CSS 변수를 못 읽음)
- [x] `algorithm.html`의 카드 본문에 다이어그램을 설명 텍스트 위에 렌더링, `.algo-image-wrap`/`.algo-illustration` 스타일 추가
- [x] **검증**: 26개 전부 `getBBox()`로 뷰박스(0 0 300 170) 밖으로 삐져나온 도형이 없는지 스크립트로 전수 검사 → 큐/우선순위 다이어그램에서 라벨 텍스트가 넘치는 것 발견해 좌표 수정 후 재검증 통과. BFS/DP/큐/해시/재귀/정렬 등 여러 개를 라이트·다크 모드 스크린샷으로 실제 렌더링 확인
- [x] (추가 요청, 2026-07-24) 파비콘/앱 아이콘 재제작 — 기존 "</>" 코드 브라켓 아이콘을 🔔 이모지 기반으로 교체, 벨이 프레임에 꽉 차면서도 중앙 정렬되도록 캔버스 렌더링(`font-size`/`y` 좌표)을 여러 차례 조정하고 진짜 투명 배경(RGBA)으로 재생성. apple-touch-icon만 예외로 브랜드색(#6367ff) 배경 유지(iOS가 투명 아이콘을 검은 배경으로 렌더링하는 제약 때문)
- [x] **검증**: 4개 아이콘 파일 전부 `sips`로 RGBA 확인, 브라우저 탭 파비콘·PWA 아이콘 스크린샷으로 중앙 정렬/크기 확인
- [x] (추가 요청) "알고핑" 브랜드 문구를 실제 사용자 접점 6곳에 적용 — 푸시 알림 제목(`server.js`의 `/api/send-daily`·`/api/resend-today` payload, `sw.js`의 fallback 문구), 테스트 알림 성공 메시지, 홈 화면 로딩 문구, 문제 제안 안내문, 일시정지 힌트, 풀이 기록 요약/빈 상태 문구
- [x] (추가 요청) PC/모바일 레이아웃 강제 전환 버튼 — `app.js`에 `initViewToggle()`/`positionButton()` 추가. `html[data-view]` 속성으로 전체 CSS를 게이팅하며, PC 모드는 사이드바(로고+세로 내비) + 넓은 본문 그리드, 모바일 모드는 기존 레이아웃 그대로. 버튼은 PC에선 사이드바 내비 아래, 모바일에선 카드와 겹치지 않도록 페이지 맨 아래 독립 섹션으로 위치 — 초기엔 내비 바로 아래 배치했다가 카드 버튼과 겹치는 문제가 있어 재조정
- [x] (추가 요청) 저장된 선택이 없을 때는 `matchMedia('(min-width: 700px)')`로 화면 폭 기반 자동 기본값 사용 (5개 페이지의 `<head>` 인라인 스크립트 + `app.js` 양쪽 동일 규칙 적용)
- [x] (추가 요청) 설정 페이지 알림/알림스케줄/필터/문제제안 4개 카드를 PC에서 왼쪽줄(알림·알림스케줄)/오른쪽줄(필터·문제제안) 독립 세로 스택으로 배치(`display:flex` 2열) — 처음엔 단순 2x2 그리드로 했다가 카드 높이가 달라 빈 공간이 생기는 문제가 있어 독립 스택으로 변경
- [x] (추가 요청) AlGoPing 제목/로고 크기 확대 — 모바일은 살짝(20→25px), PC는 크게(24→40px), 로고 아이콘도 비율에 맞춰 함께 확대
- [x] **검증**: 모바일(375px)·데스크톱(1280px) 양쪽에서 PC/모바일 토글 클릭 테스트, 화면폭 기반 자동 기본값(저장값 없을 때) 동작 확인, 설정 페이지 2열 독립 스택 스크린샷 확인
- [x] (추가 요청) 코드 품질 점검 6건 수정 — ① `index.html`/`solved.html`의 문제 링크 `href`에 `escapeHtml()` 누락 수정(URL 이스케이프) ② `skipToday()`/`markSolved()`/초기화 버튼에 `res.ok` 체크 추가해 서버 에러 시에도 성공 처리되던 문제 수정, 실패 사유를 버튼 텍스트에 노출 ③ `algorithm.html`/`settings.html`의 칩을 `<div>`→`<button type="button">`로 바꿔 키보드(Enter/Space) 조작 가능하게 함 ④ 알림 요일 전체 해제 후 저장 시도 시 막고 안내 문구 표시 ⑤ `pauseUntil` 날짜 입력에 오늘 날짜를 `min`으로 설정(로컬 date로 직접 계산해 KST 자정 근처 UTC 밀림 버그 회피) ⑥ 위 ②와 동일 작업으로 겸함
- [x] **검증**: 성공 경로 회귀 없음을 스크립트로 재확인(건너뛰기/풀었음 체크 정상 동작), 요일 전체 해제 시 저장 차단 메시지 확인, `pauseUntil.min`이 로컬 오늘 날짜로 설정됨을 확인
- [x] (추가 요청, Phase 9 로드맵에서 앞당김) 통계 대시보드 — `GET /api/stats` 신규 엔드포인트(태그별 분포, 요일별 패턴, 최근 14주 캘린더, 스트릭). 이를 위해 `storage.js`에 `solvedLog: [{id, date}]` 필드 추가(파일/Postgres 양쪽, `normalize()`가 기존 `solvedIds`를 date:null로 자동 백필) 하고 `mark-solved`가 KST 날짜를 기록하도록 변경
- [x] 풀이 기록 페이지 상단에 통계 섹션 추가 — 스트릭/최고기록/총풀이 스탯 타일, 잔디밭 스타일 캘린더 히트맵(accent 색상 `color-mix()` 기반 5단계 시퀀셜 램프, 다크모드 자동 대응), 요일별 막대 차트, 태그별 막대 차트 — 외부 차트 라이브러리 없이 순수 CSS/inline HTML로 구현
- [x] **검증**: curl로 `/api/stats` 응답 구조 확인(날짜 없는 레거시 solvedIds가 총계엔 반영되고 날짜 기반 통계에선 제외되는 것 확인), 신규 mark-solved 호출 시 solvedLog에 날짜가 기록되고 스트릭/캘린더/요일별 통계에 즉시 반영되는 것 확인, 라이트·다크 모드 스크린샷으로 히트맵 색상 단계 확인
- [x] (추가 요청) 오프라인에서도 마지막 "오늘의 문제" 보이게 — `sw.js`에 `fetch` 핸들러 추가. `/api/today`와 앱 쉘(index.html/style.css/app.js/manifest/아이콘)을 네트워크 우선으로 캐싱하고, 네트워크 실패(오프라인) 시에만 캐시로 대체. 캐시 우선 방식으로 처음 구현했다가 "배포로 파일이 바뀌어도 캐시된 사용자에게는 영원히 옛 버전만 보이는" 문제를 발견해 네트워크 우선으로 변경
- [x] 오프라인으로 캐시된 응답을 받았을 때 `index.html`에 "📡 오프라인 상태예요" 배너 표시 (서비스 워커가 `X-From-Cache` 응답 헤더를 붙여서 신호를 보냄)
- [x] **검증**: 실제로 서버 프로세스를 종료해 네트워크 요청이 실패하는 상황을 만들고 페이지를 새로고침 → 서비스 워커 캐시로부터 앱 쉘과 마지막 문제가 정상적으로 뜨고 오프라인 배너가 표시되는 것을 스크린샷으로 확인, 서버 재기동 후 정상 온라인 동작도 재확인
- [x] (추가 요청) 첫 방문 온보딩 코치마크 — `localStorage`에 완료 플래그가 없으면 어느 페이지로 처음 들어오든 3단계 안내 모달(탭 구성 → PC/모바일 전환 → 알림 켜는 법) 표시, "건너뛰기"/"다음"(마지막은 "시작하기") 클릭 시 플래그 저장 후 다시 안 뜸
- [x] **검증**: 브라우저에서 플래그 삭제 후 재방문 시 모달이 뜨는 것, 3단계 진행 및 마지막 버튼 텍스트 변경, 완료 후 새로고침해도 다시 안 뜨는 것 확인
- [x] (버그 수정, 2026-07-27) PC 레이아웃 2열 그리드에서 왼쪽 카드를 펼치면 오른쪽 카드도 같은 행 높이만큼 빈 공간으로 늘어나 보이던 문제 — CSS 그리드 기본값이 `align-items: stretch`라 같은 행의 아이템을 서로 같은 높이로 늘리는 게 원인. `#algoList`/`#solvedList`/`#csList`에 `align-items: start` 추가로 해결
- [x] (버그 수정) 알고리즘 탭 상단 칩으로 카드를 열고 다른 칩을 선택(또는 선택 해제)해도 이전에 열어둔 카드가 그대로 펼쳐진 채 남아있던 문제 — `openCard()`만 있고 짝이 되는 `closeCard()`가 없어서 생긴 버그. `activeChipTag` 상태를 추적해 칩을 바꾸거나 같은 칩을 다시 눌러 해제하면 이전에 열린 카드를 확실히 닫도록 재작성. 카드 헤더를 직접 눌러 닫을 때도 칩 선택 표시가 같이 풀리도록 동기화
- [x] **검증**: 브라우저에서 PC 모드로 칩 A 선택 → 카드 A 열림 → 칩 B 선택 → 카드 A는 닫히고(빈 공간 없음) 카드 B만 열리는 것 확인, 같은 칩 재클릭 시 선택 해제되고 카드도 닫히는 것 JS로 확인, 스크린샷으로 그리드 빈 공간 문제 해소 확인
- [x] (추가 요청) CS 개념 탭 신설 — 알고리즘 탭과 동급인 5번째 상단 탭. `data/cs-concepts.json`에 프로세스/스레드·동기비동기·CPU 스케줄링·데드락·가상메모리·TCP/UDP·HTTP/HTTPS·REST API·DNS·DB 인덱스·정규화·트랜잭션/ACID·캐시/지역성·OOP 4대 특성·SOLID 원칙까지 15개 자체 작성. 알고리즘 태그와 겹치지 않도록 OS/네트워크/DB/설계 위주로 구성(자료구조·BFS/DFS 등은 이미 알고리즘 탭에 있어 제외)
- [x] `GET /api/cs-concepts` 구현(algorithms 로딩 패턴과 동일), `public/cs.html` 신규 페이지 — 칩+아코디언 상호작용은 위에서 고친 버그 없는 로직으로 처음부터 작성(이미지 없음, `?tags=` 딥링크 불필요)
- [x] 5개 페이지(오늘의 문제/풀이 기록/알고리즘/CS 개념/설정) 전부 상단 탭에 "CS 개념" 링크 추가 — 탭이 5개로 늘면서 모바일 폭에서 "풀이 기록" 등이 어색하게 줄바꿈되는 문제가 새로 생겨, `.topnav:has(a:nth-child(5))`로 5탭일 때만 글자 크기/여백을 살짝 줄여 한 줄에 맞춤(PC 사이드바는 세로 배치라 영향 없음)
- [x] **검증**: 모바일(375px)·데스크톱(1280px) 양쪽에서 CS 개념 탭 렌더링, 칩 선택/해제, 2열 그리드 빈 공간 없음, 5탭 줄바꿈 해소를 스크린샷으로 확인
- [x] (추가 요청) 알고리즘/CS 개념 탭의 "궁금한 유형" 칩 다중 선택 지원 — 기존엔 칩 하나를 선택하면 이전 선택이 자동으로 해제되는 단일 선택 방식이었는데, 여러 유형을 동시에 펼쳐볼 수 있도록 `activeChipTag`(단일 값) → `activeChipTags`(Set)로 변경. 칩을 눌러 각자 독립적으로 켜고 끌 수 있고, 다른 칩을 골라도 기존에 열어둔 카드는 그대로 유지됨
- [x] **검증**: 브라우저에서 칩 3개(BFS/DFS/DP) 연속 선택 → 3개 카드 모두 열림 확인, 그중 하나만 다시 눌러 해제 → 그 카드만 닫히고 나머지 2개는 유지되는 것을 JS로 확인. CS 개념 탭에서도 동일하게 확인
- [x] (추가 요청) 가상 사용자 인터뷰(페르소나 서브에이전트 3인 — 개발자 취업준비생 차현우/고태윤/강경래)로 핵심 가정 4가지(결정 피로로 실행이 미뤄짐 / 알림이 오면 바로 반응함 / 기업 맞춤 큐레이션이 유의미함 / 스트릭·통계가 동기부여됨) 검증 — "알림 확인"과 "실제 착수" 사이에 별도 마찰이 있다는 점을 새로 발견(가정 중 하나는 부분 기각됨), 나머지 3개 가정은 강하게 확인됨. 상세 내용은 PRD.md §16 참고
- [x] (추가 요청) 인터뷰 검증 결과(PRD §16)를 바탕으로 "즉시 반영 가능한 작은 변경" 3가지 적용 — ① 스트릭이 0일 때 "🔥 0" 대신 "🌱 오늘부터 다시 시작해요"로 죄책감 프레이밍 완화(문제 정의 D 대응) ② 오늘의 문제 카드에 "💡 이 기업 코테 스타일로 골랐어요" 큐레이션 근거 문구 추가(문제 정의 C 강화) ③ 설정 페이지 기업/태그/난이도/출제순서를 "⚙️ 고급 설정"으로 접어두고 기본값으로 바로 시작 유도(문제 정의 A 대응)
- [x] (추가 요청) 위 3가지에 대한 사용자 피드백 반영 — 고급 설정이 저장된 필터가 있어도 자동으로 펼쳐지던 것을 제거해 항상 접힌 채로 시작하게 통일, 알림 스케줄 카드 안의 "일시정지"도 별도 토글로 접어서(하루 발송 문제 수까지는 항상 보이고 일시정지만 접힘) 덜 자주 쓰는 설정을 감춤 — 접힌 상태에서도 현재 일시정지 중이면 토글 라벨이 "⏸ 일시정지 중"으로 바뀌어 상태를 놓치지 않게 함
- [x] (추가 요청) 큐레이션 문구/배지 레이아웃 다듬기 — 배지+문구가 한 덩어리로 안 보이던 것을 문제명 오른쪽에 정렬시키고, "이 기업" 캡션과 기업명 배지의 위아래 순서를 사용자 요청대로 교체(문구가 위, 배지가 아래)
- [x] (추가 요청) 일시정지 토글 스타일 정리 — 고급 설정과 똑같이 굵게 처리돼 있던 것을 얇고 연한 톤으로 바꾸고, 위에 구분선을 넣어 "하루 발송 문제 수" select와 붙어 보이던 문제 해결. 문제명(`.name`) 폰트 크기도 확대(모바일 22→26px, PC 26→30px)
- [x] (버그 수정) 다크모드에서 태그·통계 타일·태그별 분포 막대·잔디밭 히트맵·기업 배지의 배경이 카드 배경과 거의 같은 색이라 안 보이던 문제 — `--chip-bg`를 직접 조정하는 대신(전에 텍스트 대비 때문에 어둡게 맞춰둔 값이라 재조정 시 다른 곳이 깨질 위험) `.meta span`/`.stat-tile`/`.tag-bar-track`/`.heatmap-cell`/`.company-badge`에 전부 `border: 1px solid var(--border)` 추가로 해결
- [x] **검증**: 라이트·다크 모드 양쪽에서 오늘의 문제/풀이 기록/설정 페이지를 스크린샷으로 재확인 — 배지·태그·통계 타일·히트맵 전부 다크모드에서도 경계가 뚜렷이 보이고, 라이트 모드는 기존과 동일하게 깨끗한 것 확인. 설정 페이지 고급 설정/일시정지 토글이 각각 독립적으로 접히고 펼쳐지는 것, 필터가 저장돼 있어도 새로고침 시 항상 접힌 채로 시작하는 것 확인
- [x] (버그 수정) PC 모드에서 풀이 기록 목록의 문제명까지 오늘의 문제 카드와 같이 30px로 커져 보이던 문제 — `.name` 클래스를 오늘의 문제 카드(`.card`)와 풀이 기록 목록(`.solved-item`) 둘 다 공용으로 쓰는데, 데스크톱 확대 규칙이 `html[data-view="desktop"] .name`처럼 범위를 안 좁혀서 생긴 문제였음. `html[data-view="desktop"] .card .name`으로 범위를 좁혀 오늘의 문제 카드만 커지게 수정
- [x] **검증**: PC 모드에서 `.solved-item .name`이 16px(원래 크기)로 유지되고 `.card .name`은 30px로 유지되는 것을 JS로 확인, 스크린샷으로 풀이 기록 목록 크기가 자연스러워진 것 확인

## Phase 7 — 문제 제안 → GitHub PR 자동화
- [x] 문제 제안 폼 UI (완전 공개) — `public/suggest.html` 신규 페이지. 메인 4탭에는 넣지 않고 설정 탭의 "문제 제안" 섹션에서만 진입(요청대로 "별도 창"으로 분리, 상단엔 "← 설정으로 돌아가기"만 표시)
- [x] `POST /api/suggest-problem` 구현 — 필수 필드/URL 형식/난이도(1~5) 서버 유효성 검사 포함
- [x] `github-suggest.js`: 기준 브랜치 SHA 조회 → 새 브랜치 생성 → `Project/data/problems.json` 조회 후 새 문제 추가해 커밋 → PR 생성 (id 중복 시 자동으로 `-2`, `-3`... 접미사)
- [x] `GET /api/meta`에 `suggestEnabled` 플래그 추가 — `GITHUB_TOKEN` 미설정 시 프론트에서 폼을 흐리게 비활성화하고 안내 문구 표시(정상 동작, 서버가 없는 게 아니라 "아직 설정 안 됨" 상태를 그대로 보여줌)
- [x] 저장소 범위 제한 Fine-grained PAT 발급 및 `GITHUB_TOKEN` 환경변수 설정 — 사용자가 직접 발급/설정 완료(토큰 값은 채팅에 공유되지 않음)
- [x] `server.js`에 `dotenv` 연동 (`require('dotenv').config()`를 최상단에 추가) — `.env` 파일 또는 셸 `export` 어느 쪽으로 설정해도 서버가 인식하도록 보강
- [x] **검증(토큰 없이 가능한 범위)**: curl로 필수 필드 누락/잘못된 URL/난이도 범위 초과 각각의 유효성 검사 에러 확인, 토큰 미설정 시 503 + 명확한 에러 메시지 확인, 브라우저에서 설정→문제 제안 이동, 폼이 비활성화 상태로 뜨는 것과 강제 제출 시 에러가 화면에 표시되는 것까지 확인
- [x] **검증(토큰 설정 후 종단 테스트, 2026-07-28)**: `GITHUB_TOKEN` 설정 후 서버 재시작 → `GET /api/meta`에서 `suggestEnabled: true` 확인 → `POST /api/suggest-problem`으로 테스트 문제 1건 제출 → 실제 브랜치 생성·커밋·PR([`NostalgicJ/2026-SummerAICamp#1`](https://github.com/NostalgicJ/2026-SummerAICamp/pull/1)) 생성까지 전체 흐름 정상 동작 확인. 확인 후 테스트 PR은 닫음

## Phase 7.5 — 회사별 코딩테스트 경향 조사 · 문제 후보 생성 파이프라인
- [x] 파이프라인 설계: 기출 확인 → (없으면) 경향 수집 → 오리지널 문제 제작(레퍼런스 솔루션·테스트케이스·오답 코드 포함) → 경향 기록, 4단계 절차로 정리 (PRD §17)
- [x] 데이터 위치 확정: `trends/[기업명].md`(경향 조사 기록)와 `data/problem-candidates.json`(검수 전 문제 후보) — `problems.json`과 분리해 미검수 콘텐츠가 실제 발송에 섞이지 않도록 함
- [x] **시범 실행(당근마켓, 2026-07-28)**: 웹 검색으로 기출 확인 시도 → 확정 가능한 링크 없어 2~4단계 진행 → 경향 요약을 `trends/daangn-market.md`에 기록 → 오리지널 문제 "동네 인증 판정"(구현, 레벨 2) 1건을 `data/problem-candidates.json`에 생성
- [x] **검증**: 레퍼런스 솔루션과 오답 코드를 python3로 실제 실행해 테스트케이스 4개(일반·경계값·무후보·함정) 전부 확인 — 오답 코드가 함정 케이스에서 정답과 다른 출력(`b\na` vs 정답 `b`)을 내는 것도 실행으로 재현
- [x] (수정) 파일명을 영어 슬러그로, `company` 값은 축약 없이 전체 회사명으로 통일(`당근` → `당근마켓` 등) — PRD §17.4에 명명 규칙 문서화
- [x] **확대 실행**: 나머지 8개 기업(SK하이닉스/네이버/배달의민족/삼성전자/카카오/쿠팡/토스/현대오토에버)에 병렬 서브에이전트로 동일 파이프라인 적용 — 결과는 PRD §17.5 표 참고. 6개 기업은 자체 제작(문제 후보 7건 총 생성), 카카오·토스는 기출 확정(문제 후보 생성 안 함)
- [x] **검증**: 서브에이전트가 만든 SK하이닉스·배달의민족 레퍼런스 솔루션·오답 코드를 별도로 python3 재실행해 결과 일치 재확인. 카카오 기출 링크는 WebFetch로 실제 문제 페이지("[카카오 인턴] 키패드 누르기")임을 재확인. 토스 기출 링크는 페이지 존재만 확인되고 구체적 문제명은 브라우저 접근 제한(정책 차단)으로 재확인 못해 `trends/toss.md`에 미검증으로 명시
- [x] **스키마 정리**: 기존 `problems.json` 38개 전체에 `source: "기출"` 필드를 소급 추가(전부 실제 url이 있는 실존 문제라 전부 "기출") — `problem-candidates.json`의 `source`(`"기출"`/`"ai-generated"`) 필드와 의미를 통일. PRD §9.1/§9.1b에 필드 정의 문서화(섹션 번호 중복이 있던 것도 함께 정리: 9.1a CS개념 → 9.1b 문제 후보 → 9.2 구독자 순으로 재배치). `server.js`는 필드 유무를 검증하지 않는 단순 `JSON.parse` 방식이라 필드 추가가 기존 API(`/api/today`, `/api/meta` 등)에 영향 없음을 서버 재확인으로 검증
- [x] **기업별 보유 문제 수 표시**: 설정 페이지 기업 필터 칩에 `기업명 (N)` 형태로 보유 문제 수 표시 — `GET /api/meta`가 매 요청마다 `problems.json`을 다시 집계해 `companyCounts`를 반환하므로 문제가 추가/삭제되면 별도 코드 수정 없이 그대로 반영됨. `renderChips()`에 선택적 `labelFn` 파라미터를 추가해 표시 텍스트만 바꾸고 `data-value`(필터링에 쓰이는 실제 값)는 그대로 유지 — 태그/난이도 칩은 영향 없음
- [x] **검증**: 브라우저에서 설정 페이지 기업 칩이 "카카오 (7)" 등으로 정확히 표시되는 것 확인, 칩 클릭 시 `data-value`는 순수 회사명으로 유지되고 선택 토글도 정상 동작하는 것을 JS로 직접 확인
- [x] **참고(부수 발견)**: 이 기능 검증을 위해 서버를 새 셸에서 재시작하면서 `GITHUB_TOKEN`이 실제 `.env` 파일이 아니라 특정 터미널 세션의 `export`로만 설정돼 있었다는 게 드러남(재시작 후 `suggestEnabled`가 다시 `false`) — 이후 사용자가 `.env` 파일을 직접 생성(`echo "GITHUB_TOKEN=$GITHUB_TOKEN" > .env`)해 영구 해결, `suggestEnabled: true` 재확인 완료

## Phase 7.5b — 파이프라인 방식 전환: AI 창작 → 검증된 문제풀 매칭 (2026-07-29)
- [x] **방향 전환**: 오리지널 문제를 AI가 직접 창작하던 3단계를 "프로그래머스/SWEA/리트코드에서 실제 존재하는 문제를 경향에 맞춰 매칭 추천"하는 방식으로 변경 — 이전에 만든 AI 생성 문제 7건(`data/problem-candidates.json`)은 전부 폐기하고 파일 자체를 삭제. PRD §17 전면 재작성
- [x] **분류 기준 확정**: `source` 필드를 `"기출"`(그 회사가 실제로 낸 것으로 확인된 문제) / `"경향문제"`(실제 존재하지만 그 회사가 낸 근거는 없고 유형만 매칭한 문제) 두 가지로 통일 — 기존 `"ai-generated"` 값은 완전히 폐기
- [x] **기존 38개 전체 재검증**: 9개 회사 각각 병렬 서브에이전트로 WebFetch를 이용해 프로그래머스 문제 페이지의 실제 카테고리/시즌 태그를 확인 — **카카오 라벨 6개만 실제 "KAKAO BLIND RECRUITMENT" 공식 태그로 확인되어 `"기출"` 유지**, 나머지 32개(SK 2, 네이버 5, 당근 4, 배민 4, 삼성 6, 쿠팡 4, 토스 4, 카카오의 리트코드 1)는 전부 `"경향문제"`로 재분류. 특히 "다트 게임"(현대 라벨)과 "방금그곡"(SK 라벨)은 실제로는 카카오 2018 기출, "짝지어 제거하기"(당근 라벨)는 2017 팁스타운, "다리를 지나는 트럭"(쿠팡 라벨)은 2016 ICPC Korea, "조이스틱"(삼성 라벨)은 네덜란드 NWERC 2010 문제였음을 발견 — 원 curation 당시 기업 라벨이 실제 출처와 무관하게 스타일만 보고 배정됐던 것으로 확인
- [x] **7개 회사 신규 매칭 추천**: SK/네이버/당근/배민/삼성/쿠팡/현대에 각각 실제 존재하는 문제 1건씩 매칭 추천(카펫/타겟 넘버/모의고사/혼자서 하는 틱택토/공원 산책/구명보트/소수 찾기) — 전부 WebFetch로 실제 페이지 존재와 제목·카테고리 확인 후 `problems.json`에 직접 반영(`source: "경향문제"`). 당근에 처음 추천된 "구명보트"가 쿠팡과 중복되어 "모의고사"로 교체
- [x] **검증**: 최종 `problems.json` 45개(기출 6 + 경향문제 39) id/url 중복 없음 확인, 서버가 파일을 다시 읽어 `/api/meta`의 `companyCounts`에 새 개수(카카오7/삼성7/네이버6/쿠팡5/배민5/당근5/토스4/현대3/SK3)가 즉시 반영되는 것 확인
- [x] **문서화**: PRD §17을 새 방식으로 전면 재작성(§17.5에 재분류 결과 표 추가), §9.1의 `source` 필드 설명 갱신, §9.1b(문제 후보 스키마) 섹션 삭제. 9개 `trends/*.md`에 각각 "파이프라인 방식 변경" + "기존 문제 재분류 결과" 섹션 추가

## Phase 7.5c — 기업별 문제 수 밸런싱 (2026-07-29)
- [x] 최소 보유 수가 6개 이하인 6개 기업(SK 3, 현대 3, 토스 4, 당근 5, 배민 5, 쿠팡 5)에 병렬 서브에이전트로 추가 매칭 추천 진행 — 목표: 카카오·삼성(7개)에 최대한 가깝게 최소 6개까지 채우기
- [x] **중복 발견 및 수정**: 병렬 진행 중 서로 다른 담당 에이전트가 같은 문제를 중복 추천한 사례 3건 발견(신고 결과 받기: SK·배민, 예산: 현대·토스, 숫자 문자열과 영단어: SK·현대) — 각각 한쪽에 배정하고 다른 쪽은 대체 문제로 교체(SK: 메뉴 리뉴얼·괄호 회전하기 추가 확보, 현대: 카카오프렌즈 컬러링북·튜플 유지)
- [x] **최종 반영**: `problems.json`에 11건 추가(SK +3, 현대 +3, 토스 +2, 당근 +1, 배민 +1, 쿠팡 +1) → 총 56개(기출 6 + 경향문제 50), 전 기업 6~7개로 균등화. id/url 전체 고유성 확인, 서버 재확인으로 `/api/meta` companyCounts에 즉시 반영되는 것 확인
- [x] **토스 "멋쟁이 숫자" 미검증 상태 해소(2026-07-30)**: WebFetch로는 toss.im의 클라이언트 렌더링 문제 본문을 가져올 수 없어 미검증으로 남아있었으나, WebSearch로 다른 경로(velog 후기 등)를 통해 실제 예시(입력 "111999333" → 출력 999)까지 구체적으로 확인 — `trends/toss.md`에서 미검증 표시 제거하고 기출로 확정
- [x] **문서화**: PRD §17.6에 밸런싱 결과 표 + 중복 이슈 기록, 관련 6개 `trends/*.md`에 "추가 매칭" 섹션 추가

## Phase 8 — 배포 및 운영
- [x] **배포 전 발견한 버그 수정**: `data/vapid.json`(웹 푸시 키)이 로컬 파일에만 저장되고 있었는데, Render 등 배포 환경은 재시작 시 파일시스템이 초기화될 수 있어 배포될 때마다 키가 바뀌어 기존 사용자 구독이 전부 무효화되는 문제 발견. `vapid.js`가 `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` 환경변수를 우선 사용하도록 수정(없으면 기존처럼 로컬 파일 생성/재사용, 로컬 개발 영향 없음 확인)
- [x] `render.yaml` 블루프린트 작성(저장소 루트) — Render에서 "New > Blueprint"로 이 저장소를 연결하면 rootDir(`Project`)/빌드·시작 커맨드/필요한 환경변수 목록이 자동 인식되어 수동 설정 단계를 줄임
- [x] `.gitignore`의 `.env.*` 와일드카드가 `.env.example`까지 계속 무시하고 있었던 것 발견 — `!.env.example` 예외 규칙 추가로 실제로 커밋되게 수정(그동안 한 번도 git에 올라간 적 없었음)
- [x] **Render Web Service 배포 완료**: `https://algoping.onrender.com` — Blueprint로 연결, 대시보드에 `DATABASE_URL`/`CRON_SECRET`/`GITHUB_TOKEN`/`VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` 전부 등록 완료
- [x] **Neon Postgres 연결 완료(2026-07-30 최종 확인)** — `DATABASE_URL` 등록. 스키마는 `CREATE TABLE IF NOT EXISTS`로 최초 연결 시 자동 생성되어 별도 마이그레이션 불필요했음. Render 배포 로그에서 "AlGoPing 서버 실행 중: ... (저장소: Postgres)" 문구를 사용자가 직접 확인 — 파일 폴백이 아니라 Postgres가 정상 연결됐음이 최종 확인됨. 로그에 함께 뜨는 libpq SSL 관련 메시지는 `pg` 라이브러리의 안내성 로그일 뿐 에러 아님(연결 자체는 `/api/settings` 라운드트립·`/api/send-daily` 등 실제 동작으로 이미 검증 완료)
- [x] **(배포 중 발견한 버그) GitHub Actions 워크플로 경로 오류 수정**: `.github/workflows/send-daily.yml`이 저장소 루트가 아니라 `Project/.github/workflows/`에 있어서 GitHub이 전혀 인식하지 못하고 있었음(Phase 6에서 스캐폴딩만 되고 실제로는 한 번도 등록된 적 없었던 상태) — 저장소 루트로 이동시켜 해결, `gh workflow list`로 정상 등록 확인
- [x] **크론 서비스 등록 완료**: 저장소 Settings > Secrets and variables > Actions에 `APP_URL`(`https://algoping.onrender.com`)과 `CRON_SECRET` 등록 완료, 워크플로 경로 수정 후 정상 인식
- [x] **검증(종단 테스트)**: 배포된 서버에서 `/`(200), `/api/meta`(companyCounts 등 정상), `/api/today`(200), `/api/settings` POST+GET 라운드트립(정상 저장/조회) 확인. `CRON_SECRET` 인증 정상 동작(값 불일치 시 401, 일치 시 200) 확인. `gh workflow run`으로 GitHub Actions → Render 배포 서버 실제 호출까지 성공(`HTTP 200`, `{"sent":0,"skipped":2}`) 확인 — 크론 자동화 전체 흐름 검증 완료
- [ ] 시크릿/PAT 등 환경변수 최종 정리 및 보안 점검 (선택 — 이미 GITHUB_TOKEN은 저장소 범위 제한 Fine-grained PAT 사용 중)
- [x] **(배포 중 발견) Render GitHub 연동 권한 문제**: 배포 로그에 "It looks like we don't have access to your repo"가 뜸 — `git push`해도 자동 배포가 안 걸리고 Manual Deploy를 매번 눌러야 했음. 사용자에게 Render 대시보드에서 GitHub 연결을 재인증하도록 안내(재발급 전까지는 push 후 수동 배포 필요)

## Phase 8.5 — 배포 후 UI 버그 수정 (2026-07-30)
- [x] **아이콘 중앙정렬·배경색 수정**: `public/icons/icon.svg`가 이모지를 텍스트 baseline(`y="75"`)으로 배치해 시각적으로 안 맞았던 것을 `dominant-baseline="central"`로 교체해 정확히 중앙 정렬. 배경색도 진한 보라(`#6367ff`)에서 사이트 태그 배경색과 같은 연한 라벤더(`#eeeafb`)로 변경. 헤드리스 Chrome으로 512×512 마스터 이미지를 렌더링한 뒤 `sips`로 192/180/32px 버전과 `favicon.ico`까지 전부 재생성
- [x] **파비콘 링크 태그 단순화**: `sizes="32x32"` 힌트를 제거하고 `type="image/png"`를 명시(일부 브라우저가 과도하게 구체적인 sizes 힌트에서 파비콘을 안 쓰는 경우가 있어 방어적으로 수정) — 6개 페이지 전체 적용
- [x] **파비콘 최종 진단**: 서버 응답(200, 올바른 content-type), CDN 캐시 여부(`cf-cache-status: DYNAMIC`, 캐시 안 됨), HTML `<link>` 태그 전부 정상 확인. 자동화 브라우저 환경은 파비콘 요청 자체를 안 해서 재현 불가했으나, 사용자가 사파리에서는 정상 표시됨을 확인 — 크롬에서만 안 보인 것은 그 컴퓨터의 크롬 파비콘 캐시 문제(서버/코드 이슈 아님)로 결론
- [x] **설정 페이지 "알림 시간" 버그 2건 수정**: (1) `<label for="notifyTime">`이 텍스트 자체를 클릭 가능하게 만들어서 `for` 속성 제거. (2) iOS Safari의 네이티브 `<input type="time">` 픽커가 CSS `width`/`box-sizing`을 따르지 않고 패널을 넘어가는 문제 — CSS로는 해결 안 돼서(로컬 크롬에서는 재현조차 안 됨) `<input type="time">`을 시/분 `<select>` 두 개로 완전히 교체. `currentSettings.notifyTime`은 그대로 `"HH:MM"` 문자열로 유지해 서버/저장 로직은 변경 없음. 로컬에서 값 변경 시 `currentSettings.notifyTime`이 정확히 갱신되는 것을 JS로 직접 확인
- [x] **알림 시간 분 단위를 5분 단위로 변경**: 분 select 옵션을 60개(00~59) → 12개(00,05,...,55)로 축소. 기존에 저장된 값이 5분 단위가 아닐 경우 가장 가까운 5분 단위로 반올림(최대 55분으로 clamp)해서 표시 및 `currentSettings.notifyTime`에도 반영 — 반올림 로직(7→05, 58→55, 2→00, 33→35) JS로 직접 검증
- [x] **Render 자동배포 미작동 근본 원인 진단**: `gh api repos/.../hooks`로 GitHub 저장소에 Render 웹훅이 전혀 등록되지 않은 것을 확인 — Render 대시보드 UI는 "Auto-Deploy: On Commit"으로 정상 표시되지만 실제 트리거 통로가 없는 상태였음(배포 로그의 "we don't have access to your repo" 메시지와 일치). Render의 GitHub 연동을 우회하기 위해 `.github/workflows/deploy-on-push.yml` 신규 작성 — `main` push 시 Render의 Deploy Hook URL을 직접 호출. 저장소 Secrets에 `RENDER_DEPLOY_HOOK` 등록 필요(사용자 진행 예정)
- [x] **일시정지 날짜 필드 빈 상태 개선 (1차, 이후 대체됨)**: `<input type="date">`에 "날짜 선택" 오버레이 텍스트를 추가했으나, 데스크톱에서는 빈 date input이 이미 자체적으로 "연도-월-일" placeholder를 표시하고 있어 오버레이 텍스트와 겹쳐 보이는 문제, iOS에서는 기존 시간 입력과 동일한 네이티브 피커 오버플로 문제가 재발함
- [x] **일시정지 날짜 필드 2차 수정 (이후 UX 사유로 되돌림)**: `<input type="date">` 자체를 폐기하고 년(선택 안함/올해/내년)·월·일 `<select>` 3개로 교체 — 오버플로 자체는 해결됐으나 사용자가 "너무 못생겼다"며 원래의 달력 클릭 방식을 요청, 되돌림
- [x] **일시정지 날짜 필드 진짜 원인 발견 및 최종 수정 (2026-07-31)**: select로 바꾼 뒤에도 "여전히 패널 밖으로 튀어나온다"는 피드백을 받고 재조사한 결과, 문제의 진짜 원인은 `<input type="date">`(혹은 select 3개) 자체가 아니라 같은 줄의 `지금 알림 끄기` 버튼(`.action-btn`)에 `min-width: 0`이 빠져 있어서, flex 레이아웃에서 버튼이 자기 텍스트 길이만큼 공간을 고정으로 차지하고 날짜 필드 쪽이 밀려나 튀어나온 것이었음(iOS 네이티브 피커 문제가 아니었음). `<input type="date">`를 되살리고 `.action-btn`에 `min-width: 0`을 추가, `.action-row`에 `flex-wrap: wrap` 안전장치도 추가(공간이 정말 부족하면 겹치지 않고 다음 줄로 자연스럽게 줄바꿈). 데스크톱(756px)·모바일(375px) 양쪽에서 오버플로 없음을 좌표 계산으로 직접 확인
- [x] **일시정지 날짜 필드 iOS 전용 placeholder 추가 (2026-07-31)**: 데스크톱 크롬은 빈 date input에 자체 "연도-월-일" 표시가 있어 커스텀 placeholder를 넣으면 겹쳐 보였지만, iOS는 빈 칸일 때 아무 표시도 안 해서 겹칠 게 없다는 점에 착안 — `@supports (-webkit-touch-callout: none)`(iOS WebKit에서만 매칭되는 CSS 기능 쿼리)로 "날짜 선택" 오버레이 텍스트를 iOS에서만 보이도록 조건부 적용. 값이 있으면 JS로 강제로 숨기고, 없으면 `style.display`를 비워 CSS 판단(iOS면 보임, 그 외엔 숨김)에 맡기는 방식으로 구현. 로컬 크롬에서 `getComputedStyle`로 `display: none`(의도대로 안 보임) 확인, 값 설정/해제 시 표시 로직이 정확히 토글되는 것도 JS로 확인 — 실제 iOS 기기에서 보이는지는 사용자 확인 필요
- [x] **일시정지 날짜 필드 iOS 오버플로 최종 해결 (2026-07-31)**: iOS placeholder까지 잘 보였지만 실기기에서 여전히 패널 밖으로 튀어나온다는 재확인 — `min-width: 0` 수정만으로는 부족했고, iOS 네이티브 date input이 페이지 CSS 너비를 아예 무시하고 내부적으로 더 넓게 렌더링하는 고질적 문제로 결론(이전에 `input[type="time"]`에서 겪은 것과 같은 종류). 버튼과 나란히 두는 걸 포기하고 `.action-row--stack` 수정자 클래스로 세로 배치(`flex-direction: column`)로 전환해 날짜 칸이 패널 전체 너비를 쓰게 함 — 좌표 계산으로 오버플로 없음 재확인
- [x] **일시정지 UX 개선 3건 (2026-07-31)**: (1) 알림 문구에서 브라우저/OS가 어차피 출처를 별도로 표시해주므로 중복이었던 "AlGoPing" 접두어를 우리 쪽 문구(`server.js` 2곳, `public/sw.js` 폴백 2곳)에서 제거 — 단, 브라우저가 강제로 붙이는 출처 표시 자체는 웹사이트 쪽에서 끌 수 있는 방법이 없어 그대로 둠. (2) 순서상 날짜를 먼저 정하고 끄는 게 자연스러워 "날짜 선택" 필드를 "지금 알림 끄기" 버튼보다 위로 재배치. (3) 안내 문구를 "날짜를 선택하면 그 날짜까지 알림을 보내지 않아요"로 보강해 날짜 지정 시 동작(선택 안 함=무기한 조용)과 미지정 시 동작을 모두 설명
- [x] **일시정지 날짜 필드 오버플로 4차 수정: CSS 캐스케이드 버그 + overflow:hidden 안전장치 (2026-07-31)**: 세로 배치(`.action-row--stack`) 이후에도 실기기 스크린샷으로 여전히 튀어나온다는 재확인을 받음 — 원인 조사 중 `.action-row .date-input-wrap { flex: 1 }` 규칙이 `.action-row--stack .date-input-wrap { flex: none }`보다 CSS 파일에서 더 뒤에 있어 동일 우선순위 규칙 중 나중 것이 이기는 캐스케이드 규칙 때문에 의도(`flex: none`)가 실제로 적용되지 않고 있었던 버그 발견 — `.action-row--stack` 관련 규칙들을 전부 그 뒤로 재배치해 확실히 이기도록 수정, `getComputedStyle`로 `flex: "0 0 auto"`(의도대로 적용됨) 확인. 다만 로컬 크롬은 이 버그가 있어도 오버플로가 재현되지 않았던 걸 보면 실제 원인은 iOS 고유의 네이티브 렌더링 문제일 가능성이 높다고 보고, `.date-input-wrap`에 `overflow: hidden`을 추가해 내부 렌더링이 아무리 넓어져도 박스 경계를 절대 벗어나지 못하게(강제로 잘리게) 하는 안전장치를 추가함 — 이번에도 실기기 재확인 필요
- [x] **요일 칩 정렬 + 풀이 기록 0개 메시지 중복 제거 (2026-07-31)**: (1) `#dayChips .chip`에 `flex: 1`을 줘서 일~토 7개 칩이 컨테이너 전체 너비에 고르게 분산되도록 수정(기존엔 왼쪽부터 채워지고 남는 공간이 오른쪽에 그대로 남아 치우쳐 보였음) — PC(800px)·모바일(375px) 양쪽에서 한 줄에 고르게 펼쳐지는 것 스크린샷으로 확인. (2) `solved.html`에서 푼 문제가 0개일 때 "지금까지 알고핑과 함께 0개 풀었어요"와 "아직 알고핑에서 푼 문제가 없어요."가 동시에 뜨던 중복을 없애고, 0개일 때는 `solvedCount`에 "아직 푼 문제가 없어요" 한 줄만 표시하도록 통합 — 신규 clientId(0개 상태)로 직접 확인

## Phase 9+ — 향후 로드맵 (Optional)
- [x] ~~통계 대시보드 (스트릭, 기업/태그별 풀이 현황)~~ — Phase 6에 앞당겨 구현 완료 (2026-07-27)
- [ ] 다중 알림 채널 (이메일/카카오톡 등)
- [ ] 난이도 자동 조정 추천
- [ ] 관리자 승인 플로우 (PR 없이 빠른 반영)
